from cache import Cache

class Program:
    @staticmethod
    def main(args):
        print("MAX_CAPACITY is " + str(Cache.get_max_capacity()))

        print("MAX_CAPACITY is " + str(Cache.get_max_capacity()))

        print("MAX_CAPACITY is " + str(Cache.get_max_capacity()))

        input() #he console closes immediately after execution. so it Waits for the user to press Enter





if __name__ == "__main__":
    Program.main([])
